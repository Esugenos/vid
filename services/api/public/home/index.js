const main = document.getElementById("main");

const hcount = 64, vcount = 36;
main.addEventListener('mousemove', e => {
    const x = e.offsetX, y = e.offsetY;
    const { width, height } = e.target.getBoundingClientRect();
    
});